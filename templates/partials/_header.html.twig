<div class="fixed navbar bg-base-100 z-50">
    <div class="navbar-start">
        <div class="dropdown">
            <label tabindex="0" class="btn btn-ghost lg:hidden">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" /></svg>
            </label>
            <ul tabindex="0" class="menu menu-compact dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box w-52 font-semibold">
                <li><a href="{{ path('app_home') }}">Accueil</a></li>
                <li tabindex="0">
                    <a href="{{ path('app_camping') }}" class="justify-between">
                        Logements
                        <svg class="fill-current" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><path d="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z"/></svg>
                    </a>
                    <ul class="p-2 bg-base-100">
                        <li><a href="{{ path('app_camping_mobile_home') }}">Mobiles Homes</a></li>
                        <li><a href="{{ path('app_camping_caravan') }}">Caravanes</a></li>
                        <li><a href="{{ path('app_camping_place') }}">Emplacements</a></li>
                    </ul>
                </li>
                <li><a>Plan</a></li>
                {% if app.user %}
                    {% if is_granted('ROLE_OWNER') %}
                        <li><a href="{{ path('app_owner') }}" class="text-warning">Gestion</a></li>
                    {% elseif is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('app_admin') }}" class="text-warning">Gestion</a></li>
                    {% endif %}
                {% endif %}
            </ul>
        </div>
        <a href="{{ path('app_home') }}" class="btn btn-ghost normal-case text-xl">{{ company }}</a>
    </div>
    <div class="navbar-center hidden lg:flex">
        <ul class="menu menu-horizontal px-1 font-semibold">
            <li><a href="{{ path('app_home') }}">Accueil</a></li>
            <li tabindex="0">
                <a href="{{ path('app_camping') }}">
                    Logements
                    <svg class="fill-current" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"><path d="M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z"/></svg>
                </a>
                <ul class="p-2 bg-base-100 shadow-lg">
                    <li><a href="{{ path('app_camping_mobile_home') }}">Mobiles Homes</a></li>
                    <li><a href="{{ path('app_camping_caravan') }}">Caravanes</a></li>
                    <li><a href="{{ path('app_camping_place') }}">Emplacements</a></li>
                </ul>
            </li>
            <li><a>Plan</a></li>
            {% if app.user %}
                {% if is_granted('ROLE_OWNER') %}
                    <li><a href="{{ path('app_owner') }}" class="text-warning">Gestion</a></li>
                {% elseif is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('app_admin') }}" class="text-warning">Gestion</a></li>
                {% endif %}
            {% endif %}
        </ul>
    </div>
    <div class="navbar-end">
        {% if not app.user %}
            <a href="{{ path('app_login') }}" class="btn">Connexion</a>
        {% else %}
            <a href="{{ path('app_logout') }}" class="btn">Déconnexion</a>
        {% endif %}
    </div>
</div>